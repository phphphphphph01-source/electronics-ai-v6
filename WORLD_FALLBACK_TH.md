# ระบบแก้ปัญหา YOLO ตรวจรูปคนอื่นไม่เจอ

เวอร์ชันนี้ **ไม่ต้องเทรน Dataset ใหม่**

ระบบทำงานเป็น 2 ขั้น:

1. ใช้ `best.pt` ของ YOLO ที่เทรนไว้เป็นตัวหลักเหมือนเดิม
2. ถ้า YOLO ไม่พบวัตถุ หรือ confidence ต่ำกว่าเกณฑ์ ระบบจะเรียก `yolov8s-world.pt` ที่แถมมากับโปรเจกต์เพื่อค้นหาแบบ zero-shot

ดังนั้นรูป LED ที่แตกต่างจากรูปใน Dataset มีโอกาสถูกตรวจพบมากขึ้น โดยไม่ต้องสร้าง label หรือ train ใหม่

## ไฟล์ที่เกี่ยวข้อง

- `models/best.pt` — โมเดล YOLO หลัก (ต้องมีในเครื่อง/เซิร์ฟเวอร์)
- `models/yolov8s-world.pt` — โมเดลสำรองที่มีอยู่แล้วในโปรเจกต์
- `app.py` — เรียก YOLO-World อัตโนมัติเมื่อ YOLO หลักพลาด

## การตั้งค่า

ค่าเริ่มต้นเปิด fallback แล้ว:

`ELECTRONICS_AI_ENABLE_WORLD_FALLBACK=1`

ถ้าต้องการปิด ให้ตั้งเป็น `0`

เกณฑ์เริ่มต้น:

- YOLO-World `conf=0.12`
- ยอมรับผลที่ `>= 0.18`

สามารถปรับผ่าน environment variables ได้โดยไม่ต้อง train ใหม่
