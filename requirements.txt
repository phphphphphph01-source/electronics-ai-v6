--extra-index-url https://download.pytorch.org/whl/cpu

Flask>=3.0,<4
Werkzeug>=3.0,<4
gunicorn>=23,<24

# CPU-only PyTorch: keeps Railway from installing CUDA runtime packages.
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# YOLO11 custom model runtime.
ultralytics==8.3.70

Pillow>=10,<12
numpy>=1.26,<2.2
PyYAML>=6.0
opencv-python-headless>=4.10,<5

# Optional CLIP fallback. It is NOT loaded unless
# ELECTRONICS_AI_ENABLE_CLIP_FALLBACK=1.
transformers>=4.45,<5
safetensors>=0.4
